import React from 'react';
import ReactDOM from 'react-dom/client';
import PagePlan from './components/PlanPage';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PagePlan />
  </React.StrictMode>
);